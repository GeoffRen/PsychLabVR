// Holds the player's SubjectId and current SessionId so that these can be accessed across scenes.
public static class PlayerState {
    
    // Default of 1 because the test Subject is ID 1.
    public static int SubjectId = 1;

    // Represents the current session.
    public static int SessionId;
}
